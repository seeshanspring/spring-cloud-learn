spring:
  application:
    name: spring-cloud-sleuth

  zipkin:
    base-url: http://localhost:9411/
  sleuth:
    sampler:
      probability: 1.0

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 10000
